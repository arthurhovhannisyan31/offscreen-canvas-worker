import { AbstractCanvasManager } from "../../../common/managers/abstract-canvas-manager";

export class FpsCanvasManager extends AbstractCanvasManager {
  constructor() {
    super();
  }

  // eslint-disable-next-line
  async draw(_payload: Message<any>): Promise<void>{
    //
  }
}
